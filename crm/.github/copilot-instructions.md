Read `AGENTS.md` first. It is the canonical, provider-neutral operating
contract for this repository. Use the role specifications in
`agents/roles/`, persist state in `feature_list.json` and `progress/`, and run
`./init.sh` before accepting a feature as complete.

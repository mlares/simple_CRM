# Legacy leader role

## Responsibility

`orchestrator.md` is the canonical role specification. This file is retained
only as a compatibility pointer for older runtime adapters.

## Protocol

Read and follow `orchestrator.md`. It exclusively owns feature state and uses
an independent `validator` after the `implementer` report exists.

## Handoff rule

Do not use this legacy role for new work.

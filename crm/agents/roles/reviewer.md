# Legacy reviewer role

## Responsibility

`validator.md` is the canonical role specification. This file is retained only
as a compatibility pointer for older runtime adapters.

## Protocol

Read and follow `validator.md`. A validator must be independent of the
implementer and writes only its validation report.

## Completion response

Do not use this legacy role for new work.

# Project conventions

Record the conventions that agents must follow here. At minimum, define:

- formatting and linting commands;
- naming and file-organization rules;
- supported runtime versions;
- logging and error-reporting conventions;
- test naming and fixture conventions;
- rules for generated files and dependencies.

Keep this document short and concrete. A convention that cannot be checked or
explained is probably not useful as an agent instruction.
